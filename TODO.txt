utxtfr Add exact personal identifier scanning to validate-secrets.py: read specific strings (email, etc.) from .env and block commits containing those exact values in file contents
oxmeql There should be a script for initializing a new docker image from scratch, with the correct name
tcmtfr Implement scripts/monitor/forensic-snapshot.sh: SSH to VPS, collect system info, logs, configs, file tree, open ports, and sha256 checksums into ~/.forensics/plum/YYYY-MM-DD-HHMMSS/
agkdfw Set up plum-readonly user on VPS: rbash shell, read-only group access to logs/configs/web root, separate SSH key (VPS_RO_SSH_KEY in .env)
egpocj Investigate if having issues as separate files in git is stupid
eamrfz Add dry-run mode (--dry-run) to deploy scripts that shows what would change without touching VPS
gdkjkm Add confirmation prompts to deploy scripts (y/N before touching VPS)
uoybwn Write scripts/monitor/claude-usage.sh to track Claude API usage and costs
xjnpdk Build script validation tool: scan scripts for hardcoded secrets before deployment
egmpdj Add HTTPS enforcement check: scan scripts for plaintext http:// URLs
dwohrj make sure plum-churn doesn't eat through usage
